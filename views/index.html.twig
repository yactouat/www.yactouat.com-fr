
{% extends "layout.html.twig" %}

{% block title %}{{title}}{% endblock %}

{% block mainContent %}
    <h2 class="main_heading">{{mainHeading}}</h2>
    <div class="main_container">
        {% for section in personalIntroSections %}
            {{ section|parseSection|raw }}
        {% endfor %}
    </div>
{% endblock %}
